name: Sync upstream fast

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-upstream-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: 5.x-dev

      - name: Fast-forward from upstream
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote add upstream https://github.com/matomo-org/matomo.git
          git fetch upstream 5.x-dev
          git merge --ff-only upstream/5.x-dev
          git push origin HEAD:5.x-dev

# Managed by Codex: fast scheduled upstream fork sync.
